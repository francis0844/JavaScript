// Common String Methods
let email = "braposas84@gmail.com";

// let result = email.lastIndexOf('o'); // Last letter that is use on the parameter

// let result = email.slice(0, 9); // Slicing the word depends on the parameters

// let result = email.substr(4, 10); // Same as slice

let result = email.replace('b', 'R'); // Replacing String (only the first letter encountered)

console.log(result);