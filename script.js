let age = 25; // two words that are separated with space cannot be used as a name, you can't also use variable assigner as a name, names cannot also start with number
let year = 2019;

// This is how a comment looks like

// Log things to console
console.log(age, year);

age = 30;
console.log(age);

const points = 100; // Const cannot be overridden like how let can be overridden
console.log(points);

var score = 75; // Older way of creating variables
console.log(score);

// Modern JavaScript is sometimes not compatible with older browsers when using let and const variable assigner