let age; // Undefined, not defined value
age = null; //defined value but null

//Null is use to clearout forms and to reset values

console.log(age, age + 3, `the age is ${age}`); // Undefined value/Null Value